{
    "version": 2,
    "builds": [
        {
            "src": "api/app.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "50mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/app.py"
        }
    ],
    "env": {
        "DATABASE_URL": "mysql+mysqlconnector://admin:priya@localhost:3306/emp",
        "MAIL_USERNAME": "rushideshmukh824@gmail.com",
        "MAIL_PASSWORD": "app_password"
    }
}